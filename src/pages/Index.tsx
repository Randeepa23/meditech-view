import RoleSelection from './RoleSelection';

// This component is now just a wrapper that renders the RoleSelection component
// which serves as our main landing page for the LIMS application
const Index = () => {
  return <RoleSelection />;
};

export default Index;
